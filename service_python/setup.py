from setuptools import setup, find_packages

setup(
    name="service_python",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies akan diinstall dari requirements.txt
    ],
)

