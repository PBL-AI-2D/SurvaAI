import React from "react";

// Minimal placeholder page component so Next.js treats this file as a valid module.
// This file was previously empty which caused the build to fail with a "not a module" error.
// Replace with the real admin training UI when available.
export default function Page() {
	return null;
}
